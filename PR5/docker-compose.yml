

services:
  mosquitto:
    image: eclipse-mosquitto:latest
    container_name: smart_home_broker
    ports:
      - "1883:1883"
      - "9001:9001"
    volumes:
      - ./mosquitto.conf:/mosquitto/config/mosquitto.conf
      - ./data:/mosquitto/data
    restart: unless-stopped